---
layout: null
title: Urdu Dictionary
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@3.4.1/dist/tailwind.min.css" rel="stylesheet">
  <link href="/styles.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-br from-blue-50 to-purple-100 min-h-screen flex flex-col items-center py-10">
  <h1 class="text-5xl font-extrabold mb-10 text-purple-700 drop-shadow">Urdu Dictionary</h1>
  <div class="w-full max-w-5xl bg-white/80 shadow-2xl rounded-2xl p-8 backdrop-blur">
    <table class="min-w-full table-auto border-collapse rounded-xl overflow-hidden">
      <thead>
        <tr class="bg-gradient-to-r from-blue-200 to-purple-200 text-purple-900">
          <th class="px-6 py-3 text-left text-lg font-semibold">Romanized</th>
          <th class="px-6 py-3 text-left text-lg font-semibold">Urdu</th>
          <th class="px-6 py-3 text-left text-lg font-semibold">Definition</th>
          <th class="px-6 py-3 text-left text-lg font-semibold">Usage</th>
          <th class="px-6 py-3 text-left text-lg font-semibold">Related Words</th>
        </tr>
      </thead>
      <tbody>
        {% for word in words %}
        <tr class="border-b last:border-0 hover:bg-purple-50 transition">
          <td class="px-6 py-4 text-base">{{ word.romanized }}</td>
          <td class="px-6 py-4 text-2xl font-serif">{{ word.urdu }}</td>
          <td class="px-6 py-4">{{ word.definition }}</td>
          <td class="px-6 py-4 text-gray-700 italic">{{ word.usage }}</td>
          <td class="px-6 py-4">
            {% for rel in word.related %}
              <a href="#" class="text-purple-600 hover:underline mr-2">{{ rel }}</a>
            {% endfor %}
          </td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
    <div class="mt-10 text-right">
      <a href="/add/" class="inline-block bg-purple-600 text-white px-5 py-2 rounded-lg shadow hover:bg-purple-700 transition">Add a New Word</a>
    </div>
  </div>
  <footer class="mt-12 text-gray-400 text-sm">&copy; 2024 Simple Urdu Dictionary</footer>
</body>
</html>
